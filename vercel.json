{
  "version": 2,
  "builds": [
    {
      "src": "api/finalServer.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/finalServer.py" }
  ]
}
